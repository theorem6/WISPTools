# PayPal Configuration
# Get these from https://developer.paypal.com/dashboard/applications/sandbox
# For production, use Live credentials
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_ENVIRONMENT=sandbox
# Options: 'sandbox' or 'live'

# MongoDB (if not already set)
MONGODB_URI=your_mongodb_connection_string

# Server Port
PORT=3001

# Public base URL for API (used for deployment photo URLs when using GridFS)
# Example: https://api.yourdomain.com or https://YOUR_PROJECT.run.app
# API_BASE_URL=https://api.yourdomain.com

# Firebase (if using service account file)
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_CLIENT_EMAIL=your_service_account_email
# FIREBASE_PRIVATE_KEY=your_private_key

# Internal API key - same value as in Firebase Cloud Function userTenants (env INTERNAL_API_KEY).
# Used by /api/internal/* routes called by the userTenants function.
# Generate a long random string, e.g. openssl rand -base64 32
# INTERNAL_API_KEY=your_shared_secret_here
